#[cfg(test)]
mod tests {
    use starknet::ContractAddress;
    use dojo::world::{IWorldDispatcher, IWorldDispatcherTrait};
    use instadojo::components::{Profile, Post};
    use instadojo::systems::{IProfileSystemDispatcher, IProfileSystemDispatcherTrait};

    #[test]
    #[available_gas(30000000)]
    fn test_create_profile() {
    }

    #[test]
    #[available_gas(30000000)]
    fn test_create_post() {
    }

    #[test]
    #[available_gas(30000000)]
    fn test_follow_system() {
    }
}
